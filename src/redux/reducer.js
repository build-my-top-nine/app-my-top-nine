const initialState = {
    userId: null,
    isLoggingIn: false,
    logInSuccess: false,
    logInFail: false
    
}

export default function (state = initialState, action) {
    switch(action.type)

    return state
}